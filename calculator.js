const sum = require('./sum.js');
const mult = require('./mult.js');
const div = require('./div.js');
const sub = require('./sub.js');

module.exports = {
    sum: sum,
    mult: mult,
    div: div,
    sub: sub,
}